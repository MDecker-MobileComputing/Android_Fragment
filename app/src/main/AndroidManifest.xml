<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="de.mide.fragmente"
          android:installLocation="auto"
          android:versionCode="12"
          android:versionName="1.11 (Oktober 2019)" >


    <application android:allowBackup="true"
        		 android:icon="@drawable/ic_launcher"
        		 android:label="@string/app_name"
        		 android:theme="@style/AppTheme" >

        <activity android:name="de.mide.fragmente.MainActivity"
            	  android:label="@string/app_name" >

            <intent-filter>
                <action   android:name="android.intent.action.MAIN"       />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <activity android:name="de.mide.fragmente.EntfernungVonBerlinActivity"
            	  android:label="@string/activity_title_entfernung_berlin"     />

        <activity android:name="de.mide.fragmente.EntfernungZweiKoordinatenActivity"
            	  android:label="@string/activity_title_entfernung_zwischen_zwei_koord" />

    </application>

</manifest>